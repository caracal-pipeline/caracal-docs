version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

  commands:
    # Copy your prebuilt HTML to where RTD expects it
    - mkdir -p _readthedocs/html
    - cp -r _build/html/* _readthedocs/html/
